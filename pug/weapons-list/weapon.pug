include mixins.pug

link(rel="stylesheet" href="../../style/weapons-list.css")

head
    meta(charset="UTF-8")
body
    table
        tr
            th(rowspan="2") Name
            th(rowspan="2") Cost
            th Weight
            th(rowspan="2") Size
            th(rowspan="2") Type
            th(rowspan="2") Speed
            th Melee
            th Missile
            th(colspan="3" class="center") Range
            th(colspan="2" class="center") Damage
            th(rowspan="2") Knockdown
            th(rowspan="2") Source
        tr
            th (lbs.)
            th Reach
            th ROF
            th S
            th M
            th L
            th Sm-Med
            th Large
        each book in bookPriority
            - let indents = 0
            - let weapon = weaponObj[book.id]
            if weapon
                +weaponRow(weapon, book, indents)
            - indents = 1;
            each hand in handedness
                - let grip = weaponObj.grip?.[hand.id]?.[book.id]
                if grip
                    +weaponRow(grip, book, indents)
            - let ammo = weaponObj.ammunition?.[book.id]
            if ammo
                +weaponRow(ammo, book, indents)
